# ResumeReact

Minimal static landing page — just `index.html`, no build step.

The previous full resume/portfolio site (React) has been moved to
[`archive/`](archive/README.md) for reference; it is no longer built or
deployed.
